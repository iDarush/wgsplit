# Optional WireGuard snippet for the client-facing interface, usually wg1.
# Remove old split-routing iptables PostUp/PostDown rules before using this.
#
# [Interface]
# PostUp = /opt/splitvpn/scripts/apply.sh
# PostDown = /opt/splitvpn/scripts/remove.sh
#
# Prefer systemd splitvpn.service if wg0/wg1 are managed by wg-quick services:
#
#   sudo systemctl enable --now splitvpn.service
#
